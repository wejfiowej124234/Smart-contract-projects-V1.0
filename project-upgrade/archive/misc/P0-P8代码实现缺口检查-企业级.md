# P0–P8 代码实现缺口检查（企业级）

> 对照《07-升级后智能合约清单》《06-升级后目录结构》《02-资产清单》《09-安全设计-清单》《11-升级阶段清单》《12-升级流程》对**当前代码库**做逐项核查，仅列**未实现或未达标**项，供整改与发布前验收。  
> **检查方法**：grep/read 实际合约与脚本，不依赖旧审计报告结论。  
> **检查日期**：报告生成时（代码快照一致）。

---

## 一、结论摘要

| 维度 | 状态 | 说明 |
|------|------|------|
| **07 合约清单** | 1 项未实现、若干可选/占位 | 仅 **ReserveLogic/Pool 的 mintToTreasury 路径**未实现；其余核心已实现或占位 |
| **06 目录结构** | 小差异 | test/ vs tests/；frontend 无 app/admin 分目录；liquidation-bot/monitoring/audits/formal-verification 为占位 |
| **02 资产与 config** | 已对齐 | config 驱动部署、单资产形态与阶段一致 |
| **09/12** | 已落位 | 08-runbook 含 12 §7 检查清单；09 §2 防护已落位 |

**企业级结论**：P0–P8 功能与打勾已满足；07 必选缺口已补齐（mintToTreasury、SafeTransfer、FlashLoan 已实现）。其余为可选占位或 06 命名/结构小差异。

---

## 二、07 合约清单 — 未实现或未达标项

### 2.1 未实现（必选或建议实现）

| 07 要求 | 当前代码状态 | 缺口说明 |
|---------|--------------|----------|
| **ReserveLogic / Pool：mintToTreasury** | ✅ 已实现 | 已在 `cumulateIndexes()` 中：用 `indexOld`/新 `liquidityIndex` 与 `totalScaledSupply` 调用 `ReserveLogic.computeProtocolAccrued`，将协议份额以 **aToken mint 至 `treasuryAddress`**（Treasury 持有 aToken 债权，底层由借款人还款支撑）。 |

### 2.2 已实现（供对照，非缺口）

- **Pool**：supply, withdraw, borrow, repay；getReserveData；getUserAccountData；setUserUseReserveAsCollateral（单资产：仅允许 asset==token，useAsCollateral==false 时 revert）；setReservePause；reservePaused 在 supply/withdraw/borrow/repay 中校验；入口调 ReserveLogic/RiskEngine/InterestRate。
- **PoolConfigurator**：setReservePause 已实现（Pool + Configurator 双端）。
- **ReserveLogic**：getNormalizedIncome/getNormalizedDebt（库内）、computeProtocolAccrued（库内）；cumulateToLiquidityIndex/cumulateToBorrowIndex、updateRates。Pool 对外暴露 getNormalizedIncome()/getNormalizedDebt() 并委托 ReserveLogic。
- **UserConfiguration**：库已实现（占位级）；Pool 单资产下未接入位图，符合当前阶段。
- **RiskEngine**：validateBorrow/validateWithdraw 已在 Pool 上暴露（view）。
- **Treasury**：withdraw、setReserveFactor（按 asset）已实现；Pool.setReserveFactor、Pool.setTreasury 已存在。
- **FlashLoan**：已在 **LendingPoolImpl.executeFlashLoan** 实现（转出 → IFlashLoanReceiver 回调 → 收回 amount+fee；费率 FLASH_LOAN_FEE_BPS=9）；Events.FlashLoan、Errors.FlashLoanCallbackFailed/FlashLoanRepayFailed、IFlashLoanReceiver 已就位。
- **libs**：WadRayMath、**SafeTransfer**（Pool 已全面使用 safeTransfer/safeTransferFrom）、Errors（Pool 等全面使用）、Events（PoolEvents 继承，含 FlashLoan 事件）。

### 2.3 可选/占位（主网/L2 时补齐）

| 07 要求 | 当前代码状态 | 说明 |
|---------|--------------|------|
| DexTwapAdapter | 占位 stub，getPrice() revert | 主网/多链时实现 TWAP 源 |
| SequencerUptimeGuard | 占位 stub，isSequencerUp() revert | L2 部署时实现 |
| stableDebtToken | 未实现 | 07 列为可选 |

---

## 三、06 目录结构 — 未达标项

| 06 要求 | 当前状态 | 说明 |
|---------|----------|------|
| **tests/** | 当前为 **test/** | 子目录 unit/integration/invariants/fuzz 已齐，仅根目录命名与 06 的 tests/ 不一致 |
| **frontend/app/**、**frontend/admin/** | 当前 frontend 为单应用，无 app/、admin/ 分目录 | 06 建议分用户 DApp 与治理台，当前未拆分 |
| **liquidation-bot/** | 存在 run.ts、README；无完整 src/、strategies/、risk-controls/ 子目录 | 功能最小可用，06 建议的完整目录未全建 |
| **monitoring/**、**audits/**、**formal-verification/** | 仅 README 占位 | P10 或发布前补齐 |

**已对齐**：contracts/core|tokens|oracle|libs|interfaces、scripts/deploy/、scripts/config/、scripts/verify/、scripts/ops/、docs/08-deployment-runbook.md。

---

## 四、02 资产与 config — 已达标

| 项目 | 状态 |
|------|------|
| scripts/config/reserves.\<chainId\>.json | 已存在（如 reserves.31337.json） |
| 部署脚本读取 config 驱动 setLTV/setLiquidationThreshold | deploy.ts 已读取并应用（scripts/deploy/deploy.ts） |
| 链上为唯一真实来源 | PoolConfigurator + Pool 状态为准 |

当前仍为**单资产**形态，与 02「升级后多资产」目标有形态差，但符合 P4/P5 单资产阶段约定。

---

## 五、09 安全设计、12 §7 — 已达标

| 项目 | 状态 |
|------|------|
| 12 §7 首次部署前检查清单 | docs/08-deployment-runbook.md 已含完整清单并可打勾 |
| 09 §2 攻击面与防护（预言机、重入、权限、清算、利率、升级） | 已落位；多源/Sequencer 为占位 |
| 部署入口 | 08-runbook 指定 `scripts/deploy/deploy.ts` |

---

## 六、逐项缺口清单（仅未实现/未达标）

1. ~~**mintToTreasury 路径（07 必选）**~~ **已实现**：`cumulateIndexes()` 内调用 `computeProtocolAccrued`，以 aToken mint 至 `treasuryAddress`。

2. **06 命名/结构（低优先级）**  
   - 可选：将 **test/** 重命名为 **tests/**，或于文档中注明与 06 的差异。  
   - 可选：frontend 拆分为 app/、admin/；liquidation-bot 补全 src/、strategies/、risk-controls/。

3. **可选/占位**  
   - FlashLoan 实现（07 可选）；DexTwapAdapter、SequencerUptimeGuard 实现（主网/L2）；stableDebtToken（07 可选）。

---

## 七、检查依据与方法

- **合约**：对 `contracts/core`、`contracts/tokens`、`contracts/oracle`、`contracts/libs`、`contracts/interfaces` 做 grep/read，核对 07 表项。  
- **脚本**：`scripts/deploy/deploy.ts` 确认 config 路径与 setLTV/setLiquidationThreshold 调用。  
- **文档**：`docs/08-deployment-runbook.md` 与 project-upgrade 中 12 §7、06、07 原文对照。  
- **结论**：P0–P8 范围内与 07 的必选代码缺口已全部实现（mintToTreasury、SafeTransfer、FlashLoan）；其余为占位或可选/结构差异。

---

*本报告为代码级企业级检查结果，用于发布前验收与整改优先级排序。*
